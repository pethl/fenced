
<header class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
	<link rel="shortcut icon" type="image/png" href="favicon.ico" />
	  <%= link_to "Fence Dweller", home_path, id: "logo" %>
      <nav>
        
		<ul class="nav pull-right">
		  
          <li><%= link_to "About", static_pages_about_path  %></li>
          <li><%= link_to "Contact", static_pages_contact_path  %></li>
          <li><%= link_to "Help", static_pages_help_path  %></li>

<% if signed_in? %>
		<% if current_user.admin? %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Admin <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Users", users_path %></li>
				<li class="divider"></li>
				<li><%= link_to "Dilemmas", dilemmas_path  %></li>
              </ul>
            </li>
          <% else %>
               <% end %>
          <% end %>


          <% if signed_in? %>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                User: <%= current_user.twittername%> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "View Profile", current_user %></li>
            	<li><%= link_to "Update Profile", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
            <li><%= link_to "Sign Up", signup_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>